import * as flsFunctions from "./modules/functions.js"; // * - means import all functions thats were placed overthere (flsFunctions - naming functions) from - meas from where and the path of the file

flsFunctions.isWebP();

import Swiper, { Navigation, Pagination } from 'swiper'; // modern JS codes is possible to turn on from modules folder

const swiper = new Swiper();